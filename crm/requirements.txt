# CRM Service — Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async HTTP (for calling channel service)
httpx>=0.27.0
requests>=2.31.0

# Celery + Redis
celery>=5.4.0
redis>=5.0.4

# Database — Supabase
supabase>=2.4.2

# AI — Groq SDK
groq>=0.9.0

# Data processing
pandas>=2.2.2
numpy>=1.26.4
openpyxl>=3.1.2

# Seed data generation (install locally only: pip install faker)
# faker>=25.2.0

# PDF report generation (charts + layout, pure-python)
reportlab>=4.2.0

# Embeddings via Groq API (httpx already listed above) — no local ONNX needed

# Validation & settings
pydantic[email]>=2.7.1
pydantic-settings>=2.2.1
python-multipart>=0.0.9

# Date/time
python-dateutil>=2.9.0

# Dev / test
pytest>=8.2.0
pytest-asyncio
google-auth>=0.23.7
ruff>=0.4.4
mypy>=1.10.0
